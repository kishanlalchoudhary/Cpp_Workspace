#include<iostream>
using namespace std;

/* Templates :
template -> class -> object.
Template is also called as parametrized class.

Why use templates ?
We use template so that DRY(Don't Repeate Yourself) principle is not violated. Using Templates we can do Generic Programming

Syntax for Templates :

T can be int, float, char, etc. 
template<class T>
class vector{
    T *arr;
    public:
        vector(T *arr){
            // code
        }
};

int main(){
    vector<int> myvec(intptr);
    vector<float> myfvec(floatptr);
}

Template represent many classes

*/

int main(){

    // Templates :

    return 0;
}